# Contributor scenario-figure manifest

This is a development/GUI-regression manifest, not publication validation evidence. The figures
are generated artifacts and are intentionally not distributed with CMM.

Generated by driving the real Qt workbench under the offscreen platform, fully self-contained
inside `CMM/` (no parent-folder paths). Run from the `CMM/` directory with the project venv:

```bash
QT_QPA_PLATFORM=offscreen CMM_OUTPUT_DIR=./temp_figures_new \
    PYTHONPATH=src .venv/bin/python -m cmm.app.screenshots
QT_QPA_PLATFORM=offscreen CMM_OUTPUT_DIR=./temp_figures_new \
    PYTHONPATH=src .venv/bin/python -m cmm.app.succinate_scenario
QT_QPA_PLATFORM=offscreen CMM_OUTPUT_DIR=./temp_figures_new \
    PYTHONPATH=src .venv/bin/python -m cmm.app.genome_scale_scenario
```

The images themselves are **not tracked in git** — they are reproducible from the commands
above, and a committed copy goes stale the moment the code changes. This file is the manifest:
it says what each figure shows, so the set can be checked after regenerating. Record the CMM
commit, model fingerprint, solver/version and capabilities, condition, and output hashes whenever
the set is regenerated. Do not cite this page as evidence for numerical or biological claims;
use the versioned source tables and validation artifacts for the study instead.

## Scenario 1 — demo disease model + revert metabolism (`cmm.app.screenshots`)

| File | View |
|------|------|
| `01_model_loaded.png` | Two-branch demo model loaded; reaction browser + Simulation tab |
| `02_fba.png` | FBA — objective 10 (optimal), flux table populated |
| `03_fva.png` | FVA at fraction 0.9 — flux ranges per reaction |
| `04_flux_range_slider.png` | Flux-range filter at 50% — high-flux reactions highlighted |
| `05_revert_rmta.png` | Revert (rMTA, gene KO, QP) ranking view |
| `06_revert_mta_miqp.png` | Revert (original-MTA MIQP) ranking view |

Both revert methods agree: knocking out the disease-branch gene `g2` (reaction `R2`) is the top
normalization target moving the source state toward the healthy target.

## Scenario 2 — E. coli succinate production design (`cmm.app.succinate_scenario`)

Standalone publication figures (300 DPI, colour-blind-safe):

| File | Figure |
|------|--------|
| `fig_succinate_yield.png` | Aerobic/anaerobic theoretical molar-yield comparison with explicit CO₂-contribution disclosure |
| `fig_succinate_envelope.png` | Succinate production envelope (phenotypic phase plane) |
| `fig_succinate_fseof.png` | FSEOF amplification-target ranking |
| `fig_succinate_flux_shift.png` | WT versus growth-coupled-design flux comparison |
| `fig_succinate_network.png` | Schematic carbon-backbone flux network (currency metabolites excluded) |
| `fig_escher_wt.png` / `fig_escher_succinate.png` | Curated **Escher-layout** flux maps, diverging blue↔red, edge width ∝ \|flux\|, with colorbar |

GUI captures (e_coli_core in the workbench):

| File | View |
|------|------|
| `ecoli_01_theoretical_yield.png` | Production tab — succinate yield with CO₂ disclosure |
| `ecoli_02_production_envelope.png` | Production tab — embedded envelope figure |
| `ecoli_03_fseof_targets.png` | Production tab — embedded FSEOF figure |
| `ecoli_04_succinate_bound_edits.png` | Simulation tab — anaerobic and byproduct-knockout bound edits |
| `ecoli_05_flux_map.png` | Flux Map tab — live Escher map coloured by the engineered flux |

The Escher map is the one CMM bundles (`cmm.resources.BUNDLED_MAPS[0]`), so the scenario needs
no external data file. Its provenance and license are in `src/cmm/resources/ATTRIBUTION.md`.

## Scenario 3 — genome-scale GUI walkthrough (`cmm.app.genome_scale_scenario`)

Drives the full GUI on a user-supplied SBML model (default: the public e_coli_core).

| File | View |
|------|------|
| `genome_01_model_loaded.png` | Model loaded; full reaction browser |
| `genome_02_fba.png` | FBA — optimal objective and populated flux table |
| `genome_03_glucose_medium.png` | Glucose uptake opened via a bound edit, re-run FBA |
| `genome_04_theoretical_yield.png` | Succinate theoretical yield |
| `genome_05_production_envelope.png` | Succinate production envelope |
| `genome_06_omics_eflux2.png` | E-Flux2 omics integration and reported solver path |

## Notes

- The `fig_*.png` files are the paper-ready 300-DPI figures; the GUI captures (`*_NN_*.png`) are
  ~100-DPI screen grabs for software documentation, not main-text raster figures.
- Reaction tables now size the reaction-id column to take the slack, so long ids such as
  `Biomass_Ecoli_core` are no longer truncated on genome-scale models.
